// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

function clickhandle(respostaId,perguntaId) {
    console.log(respostaId);
    console.log(perguntaId);
    //Criando array de resposta e perguntas
    //let arrayrespostas = [1,2,3,4];
    //const arrayperguntas = [];

    //Verificando se array esta preenchido
    /*
    if (arrayrespostas.length < 1) {
        arrayrespostas.push(respostaId);
        console.log(arrayrespostas);
    } else {
        console.log("teste");
    } 
    

    arrayrespostas.forEach(myFunction);

    function myFunction(item) {
        console.log(item);
    }
    */
    //arrayrespostas.push(respostaId);

    /*
    else if (arrayrespostas.find(respostaId) === true) {
        console.log("achou");
    }
    */
}

////Verificando se array esta preenchido
//if (arrayrespostas.length < 1) {
//    arrayrespostas.push(respostaId);
//    console.log(arrayrespostas);
//} else {

//} 
